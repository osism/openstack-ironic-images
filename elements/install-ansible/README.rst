===============================
Install ansible onto the target
===============================

Ansible is installed into a local pipx environment in /root and
a set of helpful collections is added.
